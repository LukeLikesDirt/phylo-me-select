% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phylo.moran.test.R
\name{phylo.moran.test}
\alias{phylo.moran.test}
\title{Compute Moran's I test with permutation}
\usage{
phylo.moran.test(x, prox, nperm = 999, one.tailed = TRUE)
}
\arguments{
\item{x}{A named numeric vector (e.g., trait values or residuals).}

\item{prox}{A phylogenetic proximity matrix (e.g., from adephylo::proxTips()).}

\item{nperm}{Number of permutations (default = 999).}

\item{one.tailed}{Logical; whether to compute a one-tailed p-value (default = TRUE).}
}
\value{
A list with:
\describe{
\item{obs}{Observed Moran's I (Abouheif’s C)}
\item{sim}{Simulated null distribution of Moran's I}
\item{pvalue}{P-value for the test}
}
}
\description{
Performs a permutation-based test of phylogenetic autocorrelation using Abouheif's C statistic.
}
\examples{
if (requireNamespace("adephylo", quietly = TRUE)) {
  tree <- ape::rtree(20)
  trait <- stats::rnorm(20)
  names(trait) <- tree$tip.label
  prox <- adephylo::proxTips(tree, method = "Abouheif")
  phylo.moran.test(trait, prox, nperm = 99)
}
}
